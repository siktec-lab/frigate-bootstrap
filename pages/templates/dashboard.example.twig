
{{ include('header.twig') }}

{% use "blocks.twig" %}

<body class="g-sidenav-show bg-gray-200">

{{ include('sidenav.twig') }}

<main class="main-content mt-0 ps">

    {{ include('topnav.twig') }}

    <div class="container-fluid py-4 pt-0">
        <div class="card">
            <div class="card-header p-3 pb-0">
                <h5 class="d-inline-block mb-0 me-2">Dashboard Actions</h5>
                <p class="d-inline-block text-sm mb-0">
                    {# Notifications on this page use Toasts from Bootstrap. Read more details. #}
                </p>
            </div>
            <div class="card-body p-3">
                <div class="row justify-content-end">
                    <div class="col-6 col-lg-4 col-xl-3 col-xxl-2 mt-sm-0 mt-2">
                        <button class="btn btn-sm bg-gradient-info w-100 mb-0 toast-btn" type="button" data-action="refresh-stats">
                            <i class="material-icons opacity-10">autorenew</i>&nbsp;&nbsp;Refresh Stats
                        </button>
                    </div>
                </div>
            </div>
        </div>    
    </div>
    <div class="container-fluid py-4">

        {{ include('icon-cards-row.twig') }}

    </div>

</main>

<section class="d-none">
    {{ block('logout_button') }}
</section>

{{ include('footer.twig') }}